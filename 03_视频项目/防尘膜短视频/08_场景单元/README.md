# 场景单元总表

这里只测试“防尘膜已经覆盖完成”的 AI 视频素材。每个场景固定物体、环境、胶带承载面、景别、首帧和提示词，用同一输入生成 3 条 10 秒视频，观察模型随机性和废片率。

## 当前进度

| 编号 | 场景 | 严格结果 | 用途 |
| ---: | --- | --- | --- |
| 04 | [电视：完整膜返工](04_电视_墙面固定_完整膜返工/结果说明.md) | 2/3 | 案例 02 存在贯穿全高的双层膜片歧义，严格拒绝 |
| 05 | [饮水机：完整膜返工](05_饮水机_墙面固定_完整膜返工/结果说明.md) | 3/3 | 三条视频均通过五节点严格验收，结论已绑定视频哈希 |

旧错误场景、明确废片和被拒绝的关键帧统一收录在 [99_垃圾桶（可回顾）](99_垃圾桶（可回顾）/README.md)，不再与合格素材混排。

## 严格通过条件

1. 黄色胶带的承载面必须真实可见；
2. 透明膜必须从胶带边缘连续展开；
3. 不得凭空长出第二条胶带或错贴到物体上；
4. 物体、膜、胶带在 10 秒内不得变形、消失、融化或跳变；
5. 技术可播放只是底线，不代表画面合格。

关键帧必须先写入 `keyframe-review.json`，六项检查全部通过且图片哈希未变化，流水线才允许提交视频任务。未生成视频的“0/3”表示三张计划测试素材均未产出，并不等于消耗了三次视频额度。

视频返回后固定抽取 1、3、5、7、9 秒逐帧检查，并将结论与视频 SHA-256 写入 `video-review.json`。任何一帧存在破膜、漏盖、断胶、双层膜片或结构含糊，整条视频拒绝；技术完成和完整解码不能代替画面验收。

两个及以上案例必须使用独立 `variant_scenes`。每条案例分别填写物体种类、物体形态、环境场景、覆盖状态、镜头景别、镜头运动和人物/施工状态；重复提示词或重复七维组合会在读取 API Key 前被流水线拒绝。

## 新建场景的固定结构

```text
NN_物体_固定方式/
├── 结果说明.md
├── 三条对比图.jpg
├── 案例视频/
│   ├── 案例-01.mp4
│   ├── 案例-02.mp4
│   └── 案例-03.mp4
└── 技术记录（不用看）/
    ├── project.json
    ├── status.json
    ├── verify-report.json
    ├── keyframes/
    └── clips/
```
