# 饮水机完整膜返工：3/3 严格通过

## 当前结论

前三轮候选图全部被关键帧硬门拦截。随后对候选 01 做定向编辑，获得一张严格合格首帧：单张完整膜覆盖饮水机，黄胶与膜同宽、两端方正、全宽连续连接。三条任务复用该冻结首帧，只测试视频模型随机性。

三条 10.04 秒视频均已完整解码，并分别抽取 1、3、5、7、9 秒共 15 张原分辨率质检帧。严格逐张检查后，3 条全部通过：膜全程连续且完整覆盖饮水机，黄胶与膜上沿连续，未出现破洞、分叉、第二层膜、额外胶带或结构跳变。

## 可追溯证据

新 SOP 要求关键帧同时通过六项检查：膜连续无破洞、完整覆盖饮水机、胶带与膜全宽连续、胶带贴墙、只有一体式产品、无明显视觉伪影。合格首帧 SHA-256 为 `b0dd9eaf29ba78523b6d5020148610194a9a45048edf4cea8ac855d2584d525f`；三条记录均已绑定该哈希并批准。

已尝试的修正通道：

1. 常规提示词生成 3 张：全部不合格；
2. 加参考图定向生成 3 张：全部不合格；
3. 借用正确电视结构图约束再生成 3 张：仍全部不合格；
4. ChatCut 当前会话缺少 `submit_image`，无法发起定向修图；
5. AutoGLM 本地 Token 服务 `127.0.0.1:53699` 未启动；原生生图鉴权也已撤销。

视频任务严格使用 `retries=0`，任务上限 3、付费秒数上限 30，没有自动重试或重复提交。三条视频验收结论及视频 SHA-256 写入 `技术记录（不用看）/video-review.json`；质检帧保存在 `技术记录（不用看）/质检帧/`，总览见 `三条对比图.jpg`。任何视频文件被替换后，现有哈希验收都应视为失效。
