# 饮水机完整膜返工：关键帧未通过，视频未生成

## 当前结论

本批次严格结果是 **0/3，阻断于关键帧**。三轮候选图都存在胶带长于膜、胶带与膜断开、额外胶带或覆盖结构不完整等问题，因此没有提交任何视频任务，视频费用为 0。

## 为什么没有继续跑

新 SOP 要求关键帧同时通过六项检查：膜连续无破洞、完整覆盖饮水机、胶带与膜全宽连续、胶带贴墙、只有一体式产品、无明显视觉伪影。当前三张图未达到这些条件，`keyframe-review.json` 保持拒绝状态，流水线会强制报错并停止。

已尝试的修正通道：

1. 常规提示词生成 3 张：全部不合格；
2. 加参考图定向生成 3 张：全部不合格；
3. 借用正确电视结构图约束再生成 3 张：仍全部不合格；
4. ChatCut 当前会话缺少 `submit_image`，无法发起定向修图；
5. AutoGLM 本地 Token 服务 `127.0.0.1:53699` 未启动；原生生图鉴权也已撤销。

恢复条件很明确：获得一张满足六项检查的饮水机首帧，记录图片哈希并批准后，才允许用同一首帧生成 3 条 10 秒随机性测试视频。不能用现有坏图跳过门禁。
